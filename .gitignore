# Captured frames and rendered videos (default locations)
frames/
*.mp4

# Python
__pycache__/
*.pyc
*.egg-info/
build/
dist/
*.spec
reolink-timelapse-win64-*.zip
reolink-timelapse-gui-win64-*.zip
.venv/
venv/

# Config lives in the OS user-config dir, not the repo, but just in case
config.yaml
.env
