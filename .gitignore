# Captured frames and rendered videos (default locations)
frames/
*.mp4

# Program-folder-relative defaults for source runs (see config.app_root_dir)
/Timelapses/

# Python
__pycache__/
*.pyc
*.egg-info/
build/
dist/
*.spec
reolink-timelapse-win64-*.zip
reolink-timelapse-gui-win64-*.zip
.venv/
venv/

# Config defaults into the program folder for source runs too (holds
# camera credentials -- never commit it)
config.yaml
.env
